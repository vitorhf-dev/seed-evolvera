import { escapeText } from "../html.js";
import { actions, heading, media, sectionShell, type RenderContext, type SectionOf } from "./shared.js";
export const renderSplitFeature = (section: SectionOf<"splitFeature">, context: RenderContext): string => { const visual = media(section.media, context); const copy = `<div class="split-panel">${heading(section.heading, "Conhecimento aplicado")}<p>${escapeText(section.body)}</p><ul class="points">${section.points.map((point) => `<li>${escapeText(point)}</li>`).join("")}</ul>${section.action ? actions([section.action], context) : ""}</div>`; return sectionShell(section, visual ? `<div class="split">${visual}${copy}</div>` : copy); };

{
  "name": "@evolvera/industrial-site-seed",
  "version": "0.1.0",
  "private": true,
  "description": "Persistent six-page static industrial website blueprint",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "serve": "node tools/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:structure": "node --test tests/structure.test.mjs",
    "test:server": "node --test tests/server.test.mjs",
    "test:browser": "node --test tests/browser-foundation.test.mjs",
    "check": "npm test"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}

node_modules/
dist/
coverage/
.playwright/
test-results/
playwright-report/
*.tsbuildinfo
# Saída de sessão do PI: regenerável. Regras explícitas em vez de ignorar `.pi/` inteiro,
# para que conhecimento durável (skills, memory) não seja engolido em silêncio se um dia existir.
.pi/build/
.pi/recovery/
.pi/handoff/
.pi/plans/
